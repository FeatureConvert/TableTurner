# TableTurner

TableTurner converts filled Excel annotation templates into GenBank feature tables.

## Overview

This tool takes a completed Excel annotation template as input and produces a GenBank-formatted feature table as output, streamlining the process of preparing sequence annotations for submission or downstream analysis.

## Status

This project is under active development. Usage instructions and examples will be added as the tool takes shape.
